/*2. Write a function to generate random whole number between 0 to 10;*/


function random(minimum, maximum) {
	return Math.floor(Math.random() * (maximum - minimum + 1)) + minimum
}

console.log(random(0, 10))
